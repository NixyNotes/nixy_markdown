name: nixi_markdown
description: Markdown for nixi package.
version: 0.1.1
homepage: https://github.com/MuhammedKpln/nixi-markdown

environment:
  sdk: '>=3.0.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
